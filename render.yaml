services:
  - type: web
    name: clinic-patient-manager
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: APP_USER
        value: admin
      - key: APP_PASSWORD
        sync: false
      - key: MEMORY_ONLY
        value: "true"
